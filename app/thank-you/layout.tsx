import type { Metadata } from "next";

export const metadata: Metadata = { title: "طلبكِ وصل" };

export default function ThankYouLayout({ children }: { children: React.ReactNode }) {
  return children;
}
